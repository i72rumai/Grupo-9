#include <iostream>
using namespace std;

void copia_seguridad(char *nfich)
{
  FILE *nfich=NULL;
  fich=fopen(nfich,"ab");
  fich2=fopen(copia.bin,"wb");
  if(fich==NULL||fich2=NULL)
  {
    cout<<"error abriendo fichero..."<<endl;
  }
  while((fich1,)))
  {
    if()
        {

        }
  }
  fclose(fich1);
  fclose(fich2);
}
